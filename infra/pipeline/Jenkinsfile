pipeline {
    agent {
        docker {
            image 'alpine'
            args '-u root:root'
            label 'jarvis'
        }
    }
    stages {
        stage('Deps') {
            steps {
                sh 'apk add git go'
            }
        }
        stage('Build server') {
            steps {
                sh 'cd server && go build'
            }
        }
    }
}
